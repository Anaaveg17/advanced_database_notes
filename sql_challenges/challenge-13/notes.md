## Notes

Assignment history can be tracked using triggers and validity dates (`valid_from`, `valid_to`), while ETL processes move data from OLTP tables into a star schema for historical reporting and analysis.
