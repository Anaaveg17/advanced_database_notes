# Notes.

- UNION: Merges results and removes duplicates.
- UNION: ALL merges results and keeps duplicates.
- MINUS: Returns rows only from the first query (not in the second).
- INTERSECTION: Returns rows that intersect in both queries.