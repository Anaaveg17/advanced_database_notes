# Session – 2026-02-17.

## Topics covered
- Database normalization: Reduces duplicated data in any single table.
- Multi-table queries with JOINs: Primary keys identify entities, JOINs in queries combine row data across two separate tables using those keys. 

## What I understood
- Tables are a structure with columns and rows, where we can store organized information. We hace id, foreign keys, which are useful when you need to reference data from different tables. 

- There are different types of opperations that helps us work with tables. For example join. Join allows to merge data on two or more tables.
        - INNER JOIN
        - LEFT  JOIN
        - RIGHT JOIN
        - FULL JOIN

- Select is used to search some especific data from the table. "SELECT (''') FROM ('''')  WHERE ('''')"
(Conditions)

## What is still condusing:
Nothing 

## Question
None

## Related concepts
- [Concept name](../concepts/concept-name.md)

## Resources used
- See `resources/`
